# Archived docs

This directory holds legacy planning notes that refer to older file layouts or
superseded implementation details.

Current docs should point at the active `.ts` modules under `apps/`.
